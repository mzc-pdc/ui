export interface IMenuPoPsManagementProps {
  iconSize?: number;
  title: string;
}

export interface IWorkspaceIconProps {
  name: string;
  icon: string | undefined | null;
  bg: string | undefined | null;
}

import { Components } from '@mui/material/styles/components';

export const SpeedDialIconTheme = (
    theme: any
): Components['MuiSpeedDialIcon'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

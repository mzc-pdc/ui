import { Components } from '@mui/material/styles/components';

export const ImageListItemTheme = (
    theme: any
): Components['MuiImageListItem'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

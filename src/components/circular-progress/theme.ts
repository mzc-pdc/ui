import {Components} from "@mui/material/styles/components";

export const CircularProgressTheme = (theme: any): Components["MuiCircularProgress"] => (
    {
        defaultProps: {},
        styleOverrides: {

        },
    }
)

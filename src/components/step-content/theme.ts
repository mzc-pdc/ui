import { Components } from '@mui/material/styles/components';

export const StepContentTheme = (
    theme: any
): Components['MuiStepContent'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

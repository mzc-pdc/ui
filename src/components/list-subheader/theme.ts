import {Components} from "@mui/material/styles/components";

export const ListSubheaderTheme = (theme: any): Components["MuiListSubheader"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {
            }
        }
    }
);


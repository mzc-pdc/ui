import {Components} from "@mui/material/styles/components";

export const InputBaseTheme = (theme:any): Components["MuiInputBase"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {
            }
        }
    }
)
import {Components} from "@mui/material/styles/components";

export const ListItemAvatarTheme = (theme: any): Components["MuiListItemAvatar"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {}
        }
    }
);


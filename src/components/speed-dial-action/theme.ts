import { Components } from '@mui/material/styles/components';

export const SpeedDialActionTheme = (
    theme: any
): Components['MuiSpeedDialAction'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

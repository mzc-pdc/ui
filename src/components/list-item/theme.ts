import {Components} from "@mui/material/styles/components";
import {alpha} from "@mui/system";

export const ListItemTheme = (theme: any): Components["MuiListItem"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {
            },
        }
    }
);


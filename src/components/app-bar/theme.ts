import {Components} from "@mui/material/styles/components";

export const AppBarTheme = (theme: any): Components["MuiAppBar"] => (
    {
        defaultProps: {},
        styleOverrides: {
            
        },
    }
)

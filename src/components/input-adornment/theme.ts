import {Components} from "@mui/material/styles/components";

export const InputAdornmentTheme = (theme:any): Components["MuiInputAdornment"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {

            }
        }
    }
)
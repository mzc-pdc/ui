import {alpha} from '@mui/material';
import {Components} from '@mui/material/styles/components';

export const RatingTheme = (
    theme: any
): Components['MuiRating'] => ({
    defaultProps: {},
    styleOverrides: {}
})
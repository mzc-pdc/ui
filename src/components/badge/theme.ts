import {Components} from "@mui/material/styles/components";

export const BadgeTheme = (theme: any): Components["MuiBadge"] => (
    {
        defaultProps: {},
        styleOverrides: {

        },
    }
)

import { Components } from '@mui/material/styles/components';

export const SnackbarContentTheme = (
    theme: any
): Components['MuiSnackbarContent'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

import { Components } from '@mui/material/styles/components';

export const NativeSelectTheme = (
    theme: any
): Components['MuiNativeSelect'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

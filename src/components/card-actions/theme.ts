import { Components } from '@mui/material/styles/components';

export const CardActionsTheme = (
    theme: any
): Components['MuiCardActions'] => ({
    defaultProps: {},
    styleOverrides: {

    },
});

import { Components } from '@mui/material/styles/components';

export const SpeedDialTheme = (
    theme: any
): Components['MuiSpeedDial'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

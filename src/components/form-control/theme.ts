import {Components} from "@mui/material/styles/components";

export const FormControlTheme = (theme:any): Components["MuiFormControl"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {
            }
        }
    }
)
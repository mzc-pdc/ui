import { Components } from '@mui/material/styles/components';

export const StepButtonTheme = (
    theme: any
): Components['MuiStepButton'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

import {Components} from "@mui/material/styles/components";

export const MenuListTheme = (theme: any): Components["MuiList"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {

            },
        }
    }
);


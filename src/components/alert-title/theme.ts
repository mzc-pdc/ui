import {Components} from "@mui/material/styles/components";

export const AlertTitleTheme = (theme: any): Components["MuiAlertTitle"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            
        },
    }
);


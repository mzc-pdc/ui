import {Components} from "@mui/material/styles/components";

export const LinearProgressTheme = (theme: any): Components["MuiLinearProgress"] => (
    {
        defaultProps: {},
        styleOverrides: {

        },
    }
)

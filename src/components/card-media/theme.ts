import { Components } from '@mui/material/styles/components';

export const CardMediaTheme = (
    theme: any
): Components['MuiCardMedia'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

import {Components} from "@mui/material/styles/components";

export const CollapseTheme = (theme: any): Components["MuiCollapse"] => (
    {
        defaultProps: {},
        styleOverrides: {

        },
    }
)

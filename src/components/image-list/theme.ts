import { Components } from '@mui/material/styles/components';

export const ImageListTheme = (
    theme: any
): Components['MuiImageList'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

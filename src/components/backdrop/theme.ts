import {Components} from "@mui/material/styles/components";

export const BackdropTheme = (theme: any): Components["MuiBackdrop"] => (
    {
        defaultProps: {},
        styleOverrides: {

        },
    }
)

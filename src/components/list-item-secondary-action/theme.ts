import {Components} from "@mui/material/styles/components";

export const ListItemSecondaryActionTheme = (theme: any): Components["MuiListItemSecondaryAction"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: {
            }
        }
    }
);


import { Components } from '@mui/material/styles/components';

export const ImageListItemBarTheme = (
    theme: any
): Components['MuiImageListItemBar'] => ({
    defaultProps: {},
    styleOverrides: {
    },
});

import {Components} from "@mui/material/styles/components";

export const ToggleButtonGroupTheme = (theme: any): Components["MuiToggleButtonGroup"]=> (
    {
        defaultProps: {},
        styleOverrides: {
            root: ({ ownerState }) => ({
               
            }),
        },
    }
);

